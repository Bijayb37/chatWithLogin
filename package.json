{
  "name": "new",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prestart": "node ./prestart.js",
    "dev": "node server/index.js -p $PORT",
    "build": "next build",
    "start": "NODE_ENV=production node server/index.js -p $PORT"
  },
  "engines": {
    "node": "14.17.3"
  },
  "cacheDirectories": [".next/cache"],
  "dependencies": {
    "@types/react": "^17.0.0",
    "express": "^4.17.1",
    "next": "^10.0.8",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-scroll-to-bottom": "^4.1.2",
    "socket.io": "^4.0.0",
    "socket.io-client": "^4.0.0"
  }
}
